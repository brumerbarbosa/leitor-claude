{
  "name": "leitor-claude",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Ferramenta web local para ouvir, revisar e comentar respostas em Markdown",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:watch": "node --test --watch \"tests/*.test.js\""
  },
  "comentario": "Este arquivo existe apenas para rodar os testes. A aplicação não tem dependências, não passa por build e continua abrindo direto pelo index.html.",
  "engines": {
    "node": ">=18"
  }
}
